version: '3'

services:
  server:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PGPASSWORD=postgres
      - PGHOST=db
      - PGUSER=postgres
      - PGDBNAME=postgres
      - PASSPHRASE=yetanotherbucketlistapp
    volumes:
      - .:/app
    networks:
      - gorestapi
  db:
    restart: always
    image: postgres
    networks:
      - gorestapi

networks:
  gorestapi:


token_password = thisIsTheJwtSecretPassword //Do not commit to git!